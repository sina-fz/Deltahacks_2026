# Core dependencies
openai>=1.0.0
anthropic>=0.18.0

# LangChain dependencies
# Note: LangChain API changed significantly in 0.1.0+
# If using older versions, may need to adjust imports
langchain>=0.1.0,<0.3.0
langchain-openai>=0.0.5
langchain-anthropic>=0.1.0
langchain-community>=0.0.20
langchain-core>=0.1.0

# Web app dependencies
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# For BrachioGraph (if available)
# brachiograph>=0.1.0

# Development
pytest>=7.4.0
